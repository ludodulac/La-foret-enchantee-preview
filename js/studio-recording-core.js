(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.StudioRecordingCore=api})(typeof globalThis!=='undefined'?globalThis:this,function(){'use strict';
const MIME_CANDIDATES=['audio/webm;codecs=opus','audio/webm','audio/ogg;codecs=opus','audio/ogg','audio/mp4'];
function chooseMimeType(MR){if(!MR||typeof MR.isTypeSupported!=='function')return'';for(const type of MIME_CANDIDATES){try{if(MR.isTypeSupported(type))return type}catch{}}return''}
function resolveBlobType(actual,chosen,chunks){if(actual)return actual;if(chosen)return chosen;for(const x of chunks||[])if(x&&x.type)return x.type;return''}
function validateRecordedBlob(blob){if(!(blob instanceof Blob)||blob.size<1)throw Error('Enregistrement vide');return blob}
function userError(stage,cause){let e=Error(userMessage(stage,cause));e.stage=stage;e.cause=cause;return e}
function userMessage(stage,e){let name=e?.name||e?.cause?.name||'';if(stage==='get-user-media'){if(name==='NotAllowedError'||name==='SecurityError')return'Autorise le microphone pour enregistrer';if(name==='NotFoundError'||name==='DevicesNotFoundError')return'Aucun microphone disponible';if(name==='NotReadableError'||name==='TrackStartError')return'Le microphone est déjà utilisé ou indisponible';return'Impossible d’ouvrir le microphone'}if(stage==='recorder-create')return'Impossible de préparer l’enregistrement sur ce téléphone';if(stage==='recorder-start')return'Impossible de démarrer l’enregistrement';if(stage==='recorder-error')return'L’enregistrement a été interrompu';if(stage==='track-ended')return'Le microphone a été interrompu';if(stage==='decode')return'La prise a été enregistrée mais reste illisible';return'Enregistrement impossible'}
return{MIME_CANDIDATES,chooseMimeType,resolveBlobType,validateRecordedBlob,userError,userMessage}});